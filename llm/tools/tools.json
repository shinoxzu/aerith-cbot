{
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "get_chat_info",
        "description": "Получает информацию о чате: название, описание и закрепленное сообщение",
        "strict": true,
        "parameters": {
          "type": "object",
          "required": [],
          "properties": {},
          "additionalProperties": false
        }
      }
    },

    {
      "type": "function",
      "function": {
        "name": "remember_user_info",
        "description": "Запоминает информацию о пользователе: его предпочтения, друзей, где он побывал и другое",
        "strict": true,
        "parameters": {
          "type": "object",
          "required": ["info", "user_id"],
          "properties": {
            "info": {
              "type": "string",
              "description": "Информация, которую необходимо запомнить. До тысячи символов"
            },
            "user_id": {
              "type": "integer",
              "description": "user_id пользователя, о ком информация (обязательно берем ТОЛЬКО из JSON сообщения)"
            }
          },
          "additionalProperties": false
        }
      }
    },

    {
      "type": "function",
      "function": {
        "name": "fetch_user_info",
        "description": "Получает информацию о пользователе из памяти. Например, можно узнать, какого цвета его ноутбук. Не вызывай часто",
        "strict": true,
        "parameters": {
          "type": "object",
          "required": ["query", "user_id"],
          "properties": {
            "user_id": {
              "type": "integer",
              "description": "user_id пользователя, о ком информация (обязательно берем ТОЛЬКО из JSON сообщения)"
            },
            "query": {
              "type": "string",
              "description": "Что надо искать"
            }
          },
          "additionalProperties": false
        }
      }
    }
  ]
}
